{
  "name": "public-agent-provisioning",
  "version": "0.0.0",
  "description": "A forkable template that provisions coding agents from maintained tools. rulesync owns rules, skills and hooks. Trunk owns the git guards. Vale owns prose. node:test proves each guard blocks a bad input and passes a good one.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@11.17.0",
  "scripts": {
    "test": "node --test",
    "lint": "trunk check --all",
    "docs": "vale .",
    "generate": "rulesync generate",
    "verify": "rulesync generate --check && trunk check --all && vale . && node --test"
  },
  "devDependencies": {
    "rulesync": "16.14.0"
  }
}
