{
  "allow": [],
  "deny": [
    "curl *|*sh*",
    "git clean -*",
    "git commit *--no-verify*",
    "git push *--no-verify*",
    "git push --force*",
    "git push -f*",
    "git reset --hard*",
    "rm -fr *",
    "rm -r *",
    "rm -rf *",
    "wget *|*sh*"
  ],
  "allowRedirects": false
}
